# [FE-021] Console — Workspace 운영 진입점 및 Workflow 연결 UX 정비

> **Backlog**: 로그인 이후 콘솔 사용자가 워크스페이스를 기준으로 운영 흐름에 진입하고, 해당 워크스페이스 안에서 workflow 목록 조회와 업로드 화면 이동까지 자연스럽게 이어가고 싶다.
> **Layer**: Frontend (Operator Console)
> **Template**: `.agent/specs/_TEMPLATE_FE.md`
> **Branch**: `spec/021`
> **Implementation Baseline**: `feature/021-workspace-crud-ui`
> **Depends on**: `frontend/DESIGN.md`, 기존 workspace CRUD API, workspace별 workflow 목록 API, 기존 `WorkflowDraftReadPage` 읽기 흐름

---

## Goal

로그인 이후 기본 운영 진입점을 `/workspaces`로 통일하고, 워크스페이스 목록 CRUD와 워크스페이스 범위의 workflow 탐색 동선을 하나의 일관된 정보구조로 재정비한다.

---

## User Flow Chart

```mermaid
flowchart TD
    A[로그인 완료] --> B[/workspaces 진입]
    B --> C[workspace 목록 GET]
    C --> D{응답 상태}

    D -->|로딩| E[로딩 패널 표시]
    D -->|에러| F[인라인 에러 + 다시 시도]
    D -->|빈 배열| G[Empty state + 생성 CTA]
    D -->|정상| H[고정 크기 Workspace 카드 그리드]

    G --> I[CreateWorkspaceDialog 열기]
    H -->|상단 CTA| I
    I --> J[이름 입력]
    J --> K{유효성 검사}
    K -->|실패| L[이름 필드 에러 표시]
    K -->|통과| M[workspaceKey 자동 생성 후 POST /workspaces]
    M -->|성공| N[toast.success + 목록 refetch]
    M -->|실패| O[toast.error 또는 필드 에러]

    H --> P{카드 액션}
    P -->|카드 전체 클릭 또는 Open Workspace| Q[/workspaces/:workspaceId]
    Q --> R[/workspaces/:workspaceId/workflows 로 redirect]
    R --> S[workspace 정보 GET + workflow 목록 GET]
    S --> T{workflow 응답 상태}
    T -->|로딩| U[WorkspaceShell 내부 로딩 패널]
    T -->|에러| V[에러 패널 + 재시도]
    T -->|빈 배열| W[Empty state + Upload 이동 CTA]
    T -->|정상| X[workflow 카드 그리드]

    X -->|workflow 클릭| Y[/workspaces/:workspaceId/domain-packs/:packId/versions/:versionId/workflows/:workflowId]
    Y --> Z[기존 WorkflowDraftReadPage에서 상세 조회]

    W -->|Upload CTA| AA[/workspaces/:workspaceId/upload]
    AA --> AB[WorkspaceShell 내부 LogUploadForm]

    P -->|수정| AC[EditWorkspaceDialog]
    AC --> AD[이름 수정 후 PATCH /workspaces/:id]
    AD -->|성공| AE[toast.success + 목록 refetch]

    P -->|삭제| AF[ArchiveConfirmDialog]
    AF --> AG[DELETE /workspaces/:id]
    AG -->|성공| AH[toast.success + 목록 refetch]
    AH --> AI[ARCHIVED workspace는 목록에서 숨김]
```

---

## Design Diff

### As-is vs To-be

| 영역 | As-is | To-be | 변경 내용 |
|------|-------|-------|----------|
| 로그인 이후 기본 진입 | `/upload` 중심 | `/workspaces` 중심 | 운영 시작점 통일 |
| workspace 구조 | 목록 중심 진입점 부재 | 목록 -> 내부 운영 화면 진입 구조 | 정보구조 재정리 |
| workspace 생성 UX | key/name/description 직접 입력 가정 | name만 입력, key 자동 생성 | 생성 장벽 축소 |
| workspace feature 구조 | create/update/archive/list 분산 가정 | `features/workspace` 단일 슬라이스 | FSD 단순화 |
| workspace 카드 | 메타 정보 중심 | 제목/설명/명확한 CTA 중심 고정 카드 | 시인성 및 클릭 의도 강화 |
| 삭제 처리 | soft delete 의미 불명확 | 삭제 후 목록에서 즉시 숨김 | 상태 기반 UX 명확화 |
| workspace 내부 화면 | 별도 빈 홈 페이지 가능성 | `:id` 진입 시 `workflows`로 즉시 redirect | 중간 껍데기 제거 |
| workspace 내비게이션 | 상단/가로형 가정 | 좌측 세로형 `WorkspaceShell` | 운영 섹션 구분 강화 |
| workflow 연결 | workspace와 느슨한 연결 | workspace 범위 workflow 목록 조회 + detail read page 연계 | 운영 흐름 연결 |

---

## Scope

### In Scope

- `/workspaces` 목록 페이지와 hero/CTA 구조
- workspace 생성, 수정, 삭제 다이얼로그
- `ACTIVE` 상태 workspace만 노출하는 목록 로직
- workspace 카드 고정 크기/고정 레이아웃
- `/workspaces/:workspaceId -> /workspaces/:workspaceId/workflows` redirect
- `WorkspaceShell` 기반 좌측 세로 내비게이션
- workspace 범위 workflow 목록 조회 화면
- workflow가 비어 있을 때 upload 화면으로 이동시키는 CTA
- workflow 카드에서 기존 `WorkflowDraftReadPage`로 연결되는 동선 유지
- workspace 문맥에서 `UploadPage`를 `WorkspaceShell` 안에서 렌더링하는 구조

### Out of Scope

- consultation 기능 자체 설계 및 workspace-scoped consultation 확정
- `shared/ui` 공용 컴포넌트 API 변경
- login form, global upload form, shared layout 전면 리디자인
- workflow detail 화면(`WorkflowDraftReadPage`) 자체 리디자인
- backend 스키마 변경 또는 `workspaceKey` 제거

---

## Review Notes

- 예전 021 문서처럼 `workspaceKey` 수동 입력, `create-workspace/update-workspace/archive-workspace` 분리 슬라이스, `workspace/:id` 독립 홈 페이지를 전제로 리뷰하면 오탐이 난다.
- 이번 스펙은 “workspace 목록 CRUD + workspace 범위 workflow 운영 진입 구조”를 정의한다. workflow detail read page는 연결 대상이며, 상세 화면 자체의 품질 판단은 기존 workflow spec 범위에서 본다.
- 작업 트리에서 workspace 인접 변경이 함께 보일 수 있다. 특히 consultation 연결 변경은 후속 spec 후보이므로, 해당 존재 여부만으로 FE-021의 완료/미완료를 판정하지 않는다.

---

## Component Tree

```text
WorkspaceListPage (pages/workspace-list)
└── DashboardLayout
    ├── WorkspaceHero
    │   ├── Mono Eyebrow
    │   ├── Hero Title
    │   ├── Hero Description
    │   └── Create Workspace CTA
    ├── WorkspaceSectionHeader
    │   └── Section Title
    ├── WorkspaceList (features/workspace)
    │   ├── Loading Panel
    │   ├── Error Panel
    │   ├── Empty State
    │   └── WorkspaceCard × N
    │       ├── Eyebrow
    │       ├── Name
    │       ├── Description
    │       └── Footer
    │           ├── Open Workspace
    │           ├── Edit Button
    │           └── Delete Button
    ├── CreateWorkspaceDialog
    ├── EditWorkspaceDialog
    └── ArchiveConfirmDialog

WorkspaceWorkflowsPage (pages/workspace)
└── DashboardLayout
    └── WorkspaceShell (widgets/workspace-shell)
        ├── Sidebar
        │   ├── Workspace Name
        │   └── Nav
        │       ├── Workflows
        │       └── Upload
        └── Main
            ├── Loading Panel
            ├── Error Panel
            ├── Empty State
            └── Workflow Card Grid
                └── WorkflowCard × N
                    ├── Pack Meta
                    ├── Workflow Name
                    ├── Workflow Code
                    ├── Description
                    └── Open Detail CTA

UploadPage (pages/upload)
└── DashboardLayout
    └── WorkspaceShell (workspaceId가 있을 때만)
        └── LogUploadForm
```

---

## API Integration

### Endpoints

이 스펙은 신규 API 정의보다 “기존/동반 구현된 API를 프론트 동선에 맞게 소비하는 방식”을 정리한다.

| Method | Path | 용도 | 소비 위치 |
|--------|------|------|----------|
| GET | `/api/v1/workspaces` | workspace 목록 조회 | `WorkspaceListPage` |
| GET | `/api/v1/workspaces/{workspaceId}` | workspace 단건 조회 | `WorkspaceWorkflowsPage` |
| POST | `/api/v1/workspaces` | workspace 생성 | `CreateWorkspaceDialog` |
| PATCH | `/api/v1/workspaces/{workspaceId}` | workspace 수정 | `EditWorkspaceDialog` |
| DELETE | `/api/v1/workspaces/{workspaceId}` | workspace soft delete | `ArchiveConfirmDialog` |
| GET | `/api/v1/workspaces/{workspaceId}/workflows` | workspace 범위 workflow 목록 조회 | `WorkspaceWorkflowsPage` |
| GET | `/api/v1/workspaces/{workspaceId}/domain-packs/{packId}/versions/{versionId}/workflows/{workflowId}` | workflow 상세 읽기 | 기존 `WorkflowDraftReadPage` |

### Request / Response 타입

```typescript
export type WorkspaceStatus = "ACTIVE" | "ARCHIVED";
export type WorkspaceMemberRole = "OWNER" | "ADMIN" | "REVIEWER" | "OPERATOR";

export interface WorkspaceResponse {
  id: number;
  workspaceKey: string;
  name: string;
  description: string | null;
  status: WorkspaceStatus;
  myRole: WorkspaceMemberRole;
  createdAt: string;
  updatedAt: string;
}

export interface CreateWorkspaceRequest {
  workspaceKey: string;
  name: string;
  description?: string;
}

export interface UpdateWorkspaceRequest {
  name?: string;
  description?: string | null;
}

export interface WorkspaceWorkflowSummary {
  id: number;
  workflowCode: string;
  name: string;
  description: string | null;
  initialState: string | null;
  terminalStatesJson: string;
  packId: number;
  packKey: string;
  packName: string;
  versionId: number;
  versionNo: number;
  publishedAt: string | null;
  createdAt: string;
  updatedAt: string;
}
```

### Error Mapping

| 코드 | UI 처리 |
|------|---------|
| `WORKSPACE_KEY_CONFLICT` | 생성 실패 메시지 표시 |
| `WORKSPACE_INVALID_KEY` | 자동 생성 key와 서버 규칙이 충돌할 때 에러 표시 |
| `WORKSPACE_INVALID_NAME` | 이름 필드 인라인 에러 우선 처리 |
| `WORKSPACE_NOT_FOUND` | 상태 패널 또는 toast |
| `WORKSPACE_ACCESS_DENIED` | 권한 없음 메시지 |
| 기타 네트워크/알 수 없는 오류 | `mapWorkspaceActionError()` 기본 메시지 |

---

## Routing

```tsx
<Route path="/" element={<Navigate to="/workspaces" replace />} />
<Route path="/workspaces" element={<PrivateRoute><WorkspaceListPage /></PrivateRoute>} />
<Route path="/workspaces/:workspaceId" element={<PrivateRoute><Navigate to="workflows" replace /></PrivateRoute>} />
<Route path="/workspaces/:workspaceId/workflows" element={<PrivateRoute><WorkspaceWorkflowsPage /></PrivateRoute>} />
<Route path="/workspaces/:workspaceId/upload" element={<PrivateRoute><UploadPage /></PrivateRoute>} />
<Route
  path="/workspaces/:workspaceId/domain-packs/:packId/versions/:versionId/workflows"
  element={<PrivateRoute><WorkflowDraftReadPage /></PrivateRoute>}
/>
<Route
  path="/workspaces/:workspaceId/domain-packs/:packId/versions/:versionId/workflows/:workflowId"
  element={<PrivateRoute><WorkflowDraftReadPage /></PrivateRoute>}
/>
```

### 라우팅 원칙

- `workspace/:id`는 단독 홈 페이지가 아니라 하위 운영 섹션으로 보내는 alias 라우트다.
- workspace 내부 페이지는 `DashboardLayout` + `WorkspaceShell` 조합으로 렌더링한다.
- workflow detail은 별도 workspace shell 안으로 다시 감싸지지 않고, 기존 read page 흐름을 재사용한다.
- workspace 카드에서 시작된 탐색 맥락이 `workflows -> workflow detail -> back` 흐름으로 끊기지 않아야 한다.

---

## Data Flow

```text
Route: /workspaces
  -> WorkspaceListPage
     -> workspaceApi.list()
     -> ACTIVE 상태만 필터링
     -> WorkspaceList
        -> WorkspaceCard
           -> onOpen / onEdit / onDelete

CreateWorkspaceDialog
  -> validateCreateWorkspaceForm(name, "")
  -> generateWorkspaceKey(name.trim())
  -> workspaceApi.create({ workspaceKey, name })
  -> onSuccess: 목록 refetch

EditWorkspaceDialog
  -> validateUpdateWorkspaceForm(name, "")
  -> workspaceApi.update(workspaceId, { name })
  -> onSuccess: 목록 refetch

ArchiveConfirmDialog
  -> workspaceApi.archive(workspaceId)
  -> onSuccess: 목록 refetch

Route: /workspaces/:workspaceId/workflows
  -> WorkspaceWorkflowsPage
     -> parseRouteId(workspaceId)
     -> Promise.all([
          workspaceApi.get(workspaceId),
          workspaceWorkflowApi.list(workspaceId),
        ])
     -> WorkspaceShell
        -> Empty이면 Upload CTA
        -> Ready이면 workflow 카드 그리드
        -> workflow 클릭 시 workflow detail route navigate

Route: /workspaces/:workspaceId/upload
  -> UploadPage
     -> WorkspaceShell (workspaceId가 있으면)
     -> LogUploadForm(workspaceId)
```

---

## State Management

### Server State

- TanStack Query는 도입하지 않는다.
- `WorkspaceListPage`, `WorkspaceWorkflowsPage` 모두 `useState + useEffect` 기반으로 로딩/에러/성공 상태를 관리한다.
- mutation 성공 후에는 낙관적 업데이트 대신 전체 목록 재조회로 일관성을 맞춘다.
- workflow 목록 페이지에서는 workspace 정보와 workflow 목록을 `Promise.all()`로 동시 조회한다.

### Client State

- create/edit/delete dialog open state는 `WorkspaceListPage`가 소유한다.
- 선택된 수정/삭제 대상은 `WorkspaceResponse | null` 로컬 state로 관리한다.
- 카드 클릭과 버튼 클릭의 이벤트 전파를 분리해 의도치 않은 이동을 막는다.

---

## Design Constraints

`frontend/DESIGN.md` 준수:

- 인터페이스 색상은 흑백 기반으로 유지한다.
- 모노 레이블은 `figmaMono` 계열 uppercase 시각 언어를 따른다.
- CTA와 액션 버튼은 pill/circle geometry를 유지한다.
- focus outline은 dashed 2px 패턴을 훼손하지 않는다.
- 카드/다이얼로그 radius는 기존 design token 범위를 따른다.

### 공유 컴포넌트 활용

| 역할 | `shared/ui` 파일 |
|------|-----------------|
| 생성/수정 다이얼로그 | `dialog.tsx` |
| 삭제 확인 다이얼로그 | `alert-dialog.tsx` |
| 카드 | `card.tsx` |
| 빈 상태 | `empty.tsx` |
| 텍스트 입력 | `input.tsx` |
| 버튼 | `button.tsx` |
| 로딩 표시 | `spinner.tsx` |
| Toast 알림 | `sonner.tsx` |

### 구현 제약

- `shared/ui` 공용 컴포넌트를 직접 수정하지 않고, 조합과 페이지/feature 레벨 스타일로 해결한다.
- workspace spec 범위에서 `features/workspace` 단일 슬라이스를 유지한다.
- `entities/workspace`와 `entities/workflow`는 타입/API/도메인 유틸만 책임지고, UI 조합은 상위 계층에서 수행한다.
- `shared -> features/entities` 역방향 import를 만들지 않는다.

---

## Validation Rules

| 필드 | 클라이언트 검증 | 서버 검증 | 현재 UI 노출 여부 |
|------|---------------|----------|-------------------|
| workspaceKey | 이름 기반 자동 생성 결과를 사용 | 패턴 검증, 중복 체크 | 직접 입력 안 함 |
| name | 필수, 1-255자 | `@NotBlank`, `@Size(max=255)` | 생성/수정에서 사용 |
| description | 선택, max 2000자 | `@Size(max=2000)` | 현재 UI에서는 미노출 |

### 현재 클라이언트 검증 규칙

```typescript
export function validateCreateWorkspaceForm(
  name: string,
  description: string,
): WorkspaceFieldErrors {
  const errors: WorkspaceFieldErrors = {};

  if (!name.trim()) {
    errors.name = "이름을 입력해주세요.";
  } else if (name.length > 255) {
    errors.name = "이름은 255자 이하여야 합니다.";
  }

  if (description.length > 2000) {
    errors.description = "설명은 2000자 이하여야 합니다.";
  }

  return errors;
}
```

---

## 수정 대상 파일

| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `frontend/src/app/App.tsx` | modify | workspace 중심 라우팅 및 redirect 정리 |
| `frontend/src/pages/workspace-list/ui/WorkspaceListPage.tsx` | new/modify | workspace 목록 페이지 + dialog orchestration |
| `frontend/src/pages/workspace-list/ui/workspace-list-page.module.css` | new/modify | hero 및 section 스타일 |
| `frontend/src/features/workspace/index.ts` | new | unified workspace feature barrel |
| `frontend/src/features/workspace/ui/WorkspaceList.tsx` | new | 목록 상태 분기 |
| `frontend/src/features/workspace/ui/WorkspaceCard.tsx` | new | 고정 크기 카드 + 액션 버튼 |
| `frontend/src/features/workspace/ui/CreateWorkspaceDialog.tsx` | new | 자동 key 생성 기반 생성 다이얼로그 |
| `frontend/src/features/workspace/ui/EditWorkspaceDialog.tsx` | new | 이름 중심 수정 다이얼로그 |
| `frontend/src/features/workspace/ui/ArchiveConfirmDialog.tsx` | new | 삭제 확인 다이얼로그 |
| `frontend/src/features/workspace/ui/workspace-list.module.css` | new | 카드/그리드/상태 패널 스타일 |
| `frontend/src/features/workspace/ui/workspace-dialog.module.css` | new | 생성/수정 dialog 스타일 |
| `frontend/src/features/workspace/ui/archive-confirm-dialog.module.css` | new | 삭제 dialog 스타일 |
| `frontend/src/entities/workspace/model/types.ts` | new/modify | 타입, validation, key 생성, role 정규화, 에러 매핑 |
| `frontend/src/entities/workspace/api/workspaceApi.ts` | new/modify | workspace CRUD API |
| `frontend/src/entities/workspace/index.ts` | new/modify | entity barrel |
| `frontend/src/entities/workflow/api/workspaceWorkflowApi.ts` | new | workspace workflow list API |
| `frontend/src/entities/workflow/index.ts` | modify | `WorkspaceWorkflowSummary` export |
| `frontend/src/pages/workspace/ui/WorkspaceWorkflowsPage.tsx` | new | workspace 범위 workflow 목록 페이지 |
| `frontend/src/pages/workspace/ui/workspace-workflows-page.module.css` | new | workflow 목록 스타일 |
| `frontend/src/widgets/workspace-shell/ui/WorkspaceShell.tsx` | new/modify | 좌측 세로 내비게이션 쉘 |
| `frontend/src/widgets/workspace-shell/ui/workspace-shell.module.css` | new/modify | shell 스타일 |
| `frontend/src/pages/upload/ui/UploadPage.tsx` | modify | workspace 문맥 업로드 렌더링 |

---

## UX / Validation Rules

### Workspace List

- workspace hero와 카드 목록을 시각적으로 분리한다.
- 카드 사이 여백을 충분히 주고 카드 크기는 고정한다.
- 카드 내부 항목은 벽에 붙어 보이지 않도록 내부 padding을 넉넉하게 둔다.
- `workspaceKey`, `myRole`, 최근 업데이트 정보는 카드 본문에서 노출하지 않는다.
- `status !== ACTIVE` 인 workspace는 렌더링하지 않는다.

### Permissions

- 수정 버튼은 `OWNER`, `ADMIN`, 또는 role 값이 비어 있는 경우 노출한다.
- 삭제 버튼은 `OWNER`, 또는 role 값이 비어 있는 경우 노출한다.
- 버튼 클릭은 카드의 open 동작으로 전파되지 않아야 한다.

### Create / Edit Dialog

- 생성 dialog는 이름만 입력받는다.
- 설명 입력 UI는 제공하지 않는다.
- placeholder, 입력 필드, 액션 버튼은 기존 DESIGN.md의 톤과 맞춰 시인성을 확보한다.
- 생성 시 `workspaceKey`는 이름 기반 slug + 랜덤 suffix로 생성한다.
- 이름은 필수이며 255자 이하여야 한다.

### Archive Dialog

- destructive action임을 아이콘, 대비, 경고 박스로 명확히 전달한다.
- 삭제 전 안내는 박스 내부에 배치한다.
- 삭제 성공 후 soft delete 결과가 목록에서 곧바로 반영되어야 한다.

### Workflow List in Workspace

- workflow 목록 화면은 workspace 이름과 현재 섹션을 `WorkspaceShell`에 표시한다.
- workflow가 없으면 업로드로 이어지는 CTA를 보여준다.
- workflow 카드에서 pack/version 메타와 workflow 식별 정보를 함께 보여준다.
- workflow 열기 CTA는 기존 read page 라우트로 연결되어야 한다.

---

## Tests

### Test Strategy

| 구분 | 방법 | 도구 | 비고 |
|------|------|------|------|
| 수동 테스트 | 브라우저 직접 확인 | Chrome DevTools | 주요 운영 동선 검증 |
| 빌드 검증 | 정적 빌드 | `pnpm build` | 라우트/타입 깨짐 확인 |

### Test Environment

| 항목 | 값 |
|------|---|
| 환경 | `cd frontend && pnpm dev` |
| 사전 조건 | 로그인된 사용자, workspace API 사용 가능 |
| workflow 확인용 조건 | 특정 workspace에 workflow 1개 이상 또는 0개 상태 준비 |

### Happy Path

| # | 시나리오 | 사전 조건 | 조작 | 기대 결과 |
|---|---------|---------|------|----------|
| 1 | 기본 진입 | 로그인 완료 | `/` 진입 | `/workspaces`로 이동 |
| 2 | workspace 목록 조회 | ACTIVE workspace 3개 존재 | `/workspaces` 진입 | 카드 3개 렌더링 |
| 3 | workspace 생성 | 이름 입력 가능 | 생성 dialog 열기 -> 이름 입력 -> 생성 | 성공 토스트, 목록 재조회 |
| 4 | workspace 수정 | 수정 권한 있는 workspace 존재 | 수정 클릭 -> 이름 변경 -> 저장 | 성공 토스트, 카드 이름 반영 |
| 5 | workspace 삭제 | OWNER 권한 workspace 존재 | 삭제 클릭 -> 확인 | 성공 토스트, 카드 제거 |
| 6 | workspace 진입 | workspace 존재 | 카드 클릭 또는 Open Workspace | `/workspaces/:id/workflows` 이동 |
| 7 | workflow 목록 조회 | 해당 workspace에 workflow 존재 | workflows 화면 진입 | workflow 카드 목록 표시 |
| 8 | workflow 상세 이동 | workflow 1개 이상 | workflow 열기 클릭 | 기존 `WorkflowDraftReadPage` 진입 |
| 9 | upload 이동 | workflow 없음 | empty state CTA 클릭 | `/workspaces/:id/upload` 이동 |

### Error & Edge Cases

| # | 시나리오 | 조작 | 기대 결과 |
|---|---------|------|----------|
| 1 | workspace 목록 조회 실패 | 네트워크 실패 | 에러 패널 + 다시 시도 버튼 |
| 2 | 이름 없이 생성 | 생성 버튼 클릭 | 필드 에러, API 호출 안 됨 |
| 3 | 잘못된 workspaceId | `/workspaces/abc/workflows` 진입 | 잘못된 주소 메시지 |
| 4 | workflow 목록 조회 실패 | API 실패 | workflow 화면 에러 패널 표시 |
| 5 | 삭제된 workspace | 삭제 후 목록 재조회 | 렌더링되지 않음 |
| 6 | 버튼 클릭 전파 | 수정/삭제 버튼 클릭 | workspace open 이동이 함께 일어나지 않음 |

### 접근성 / 반응형

| # | 확인 항목 | 기대 결과 |
|---|---------|----------|
| 1 | 키보드 카드 접근 | Tab 후 Enter/Space | workspace 열기 동작 |
| 2 | focus 가시성 | interactive element focus | dashed 2px outline 유지 |
| 3 | 작은 화면 | 모바일/태블릿 | 카드와 shell 레이아웃이 무너지지 않음 |
| 4 | 버튼 가독성 | 생성/수정/삭제/취소 버튼 | 배경 대비와 클릭 영역 확보 |

---

## Done Criteria

- [ ] `/` 기본 진입이 `/workspaces`로 통일된다.
- [ ] `/workspaces`에서 로딩/에러/빈 상태/정상 목록을 모두 처리한다.
- [ ] `ACTIVE` 상태 workspace만 렌더링한다.
- [ ] 생성 dialog는 이름만 입력받고 key는 자동 생성해 서버로 보낸다.
- [ ] 수정 dialog는 현재 이름 수정만 지원한다.
- [ ] 삭제 성공 후 해당 workspace는 목록에서 보이지 않는다.
- [ ] workspace 카드 전체 클릭과 Open CTA 모두 동일한 진입 경로를 사용한다.
- [ ] `/workspaces/:workspaceId`는 빈 홈 없이 `workflows`로 redirect 된다.
- [ ] `WorkspaceShell`이 workspace 범위의 최소 운영 섹션을 제공한다.
- [ ] `WorkspaceWorkflowsPage`가 workspace 정보와 workflow 목록을 함께 로드한다.
- [ ] workflow empty state에서 upload 화면으로 이어지는 CTA가 있다.
- [ ] workflow 카드에서 기존 `WorkflowDraftReadPage`로 연결된다.
- [ ] `shared/ui` 공용 컴포넌트 API를 건드리지 않고 페이지/feature/entity 범위에서 해결한다.
- [ ] FSD 의존성 방향을 위반하지 않는다.

---

## Follow-up Notes

- `workspaceKey`를 장기적으로 서버 생성 책임으로 넘길지는 backend contract와 별도 합의가 필요하다.
- consultation을 workspace 하위 섹션으로 완전히 편입할지는 후속 spec에서 다루는 편이 안전하다.
- workflow detail 화면과 workspace shell의 시각적 결합을 더 강화할지 여부는 후속 UX 정리 대상이다.
