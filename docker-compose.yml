services:
  postgres:
    image: postgres:17
    container_name: init-postgres
    environment:
      POSTGRES_USER: ${DB_USER:-init}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-init}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER:-init} -d ${DB_NAME:-init}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - init-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: init-backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${DB_NAME:-init}
      SPRING_DATASOURCE_USERNAME: ${DB_USER:-init}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILE:-local}
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
      STORAGE_S3_ENDPOINT: http://minio:9000
      STORAGE_S3_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      STORAGE_S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      STORAGE_S3_PATH_STYLE: "true"
      JWT_SECRET: ${JWT_SECRET}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176}
=======
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
=======
      JWT_SECRET: ${JWT_SECRET:-init-jwt-secret-key-for-local-development-only}
>>>>>>> 4450177 (build(pipeline): restore local runtime defaults)
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176}
>>>>>>> 22b206c (build(pipeline): harden airflow local runtime)
=======
>>>>>>> 1b112d4 (build(project): restore app compose envs)
=======
      JWT_SECRET: ${JWT_SECRET}
>>>>>>> c765850 (fix(project): restore compose env forwarding)
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
<<<<<<< HEAD
<<<<<<< HEAD
      minio:
        condition: service_healthy
=======
      postgres-reconcile:
        condition: service_completed_successfully
>>>>>>> 22b206c (build(pipeline): harden airflow local runtime)
=======
>>>>>>> cfad7d8 (build(pipeline): tighten airflow runtime safeguards)
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - init-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: init-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - init-network

<<<<<<< HEAD
  minio:
    image: minio/minio:RELEASE.2025-01-20T14-49-07Z
    container_name: init-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:9000/minio/health/live || exit 1" ]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - init-network

  airflow-init:
    <<: *airflow-common
    container_name: init-airflow-init
    entrypoint: [ "/opt/airflow/scripts/init-airflow.sh" ]
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy

  airflow-apiserver:
    <<: *airflow-common
    container_name: init-airflow-apiserver
    command: [ "airflow", "api-server" ]
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:8080"
    depends_on:
      postgres:
        condition: service_healthy
      airflow-init:
        condition: service_completed_successfully
    healthcheck:
      test: [ "CMD", "curl", "--fail", "http://localhost:8080/api/v2/version" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  airflow-scheduler:
    <<: *airflow-common
    container_name: init-airflow-scheduler
    command: [ "airflow", "scheduler" ]
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      airflow-init:
        condition: service_completed_successfully

  airflow-dag-processor:
    <<: *airflow-common
    container_name: init-airflow-dag-processor
    command: [ "airflow", "dag-processor" ]
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      airflow-init:
        condition: service_completed_successfully

  airflow-triggerer:
    <<: *airflow-common
    container_name: init-airflow-triggerer
    command: [ "airflow", "triggerer" ]
    restart: unless-stopped
    profiles:
      - triggerer
    depends_on:
      postgres:
        condition: service_healthy
      airflow-init:
        condition: service_completed_successfully

volumes:
  postgres_data:
  minio_data:
  airflow_logs:
  airflow_artifacts:
  airflow_auth:
=======
volumes:
  postgres_data:
>>>>>>> 546d2ba (build(pipeline): integrate airflow into root compose stack)


networks:
  init-network:
    driver: bridge
