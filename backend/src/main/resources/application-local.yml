spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/init
    username: app
    password: password
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  liquibase:
    enabled: true

logging:
  level:
    org.springframework.jdbc: DEBUG
    liquibase: DEBUG
