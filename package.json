{
  "name": "ajou-2026-1-capstone-5",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "10.33.0",
      "onFail": "error"
    }
  },
  "scripts": {
    "preinstall": "node -e \"const ua=process.env.npm_config_user_agent||''; if(!ua.startsWith('pnpm/')){console.error('Use pnpm for root dependencies. Run: corepack enable && pnpm install'); process.exit(1)}\"",
    "prepare": "husky",
    "env:check": "test -f .env || (echo 'Missing .env. Run: cp .env.example .env' && exit 1)",
    "dev": "pnpm run env:check && pnpm run build:backend:jar && docker compose up -d",
    "check": "pnpm run check:backend && pnpm run check:frontend && pnpm run check:ml",
    "codegen": "pnpm run env:check && pnpm run codegen:openapi && pnpm run codegen:frontend",
    "smoke": "pnpm run env:check && (running=\"$(docker compose ps --status running --services postgres backend frontend airflow-apiserver | sort | tr '\\n' ' ')\"; test \"$running\" = \"airflow-apiserver backend frontend postgres \" || (echo \"Smoke failed. Expected running services: airflow-apiserver backend frontend postgres. Actual: ${running:-none}\" && exit 1))",
    "logs": "pnpm run env:check && docker compose logs -f backend frontend airflow-apiserver",
    "format": "prettier --write .",
    "format:check": "prettier --check package.json lint-staged.config.js .github/workflows/*.yml",
    "build:backend:jar": "cd backend && ./gradlew bootJar",
    "format:backend": "cd backend && ./gradlew spotlessApply",
    "format:backend:check": "cd backend && ./gradlew spotlessCheck",
    "lint:backend": "cd backend && ./gradlew checkstyleMain checkstyleTest",
    "test:backend": "cd backend && ./gradlew test",
    "test:backend:h2": "cd backend && ./gradlew testH2",
    "test:backend:pg": "cd backend && ./gradlew testPg",
    "test:backend:coverage": "cd backend && ./gradlew test jacocoTestCoverageVerification",
    "ci:backend": "cd backend && ./gradlew test jacocoTestCoverageVerification testPg build -x checkstyleMain -x checkstyleTest",
    "ci:backend:sonar": "cd backend && ./gradlew test jacocoTestReport sonar --info",
    "build:frontend": "pnpm --dir frontend build",
    "test:frontend": "pnpm --dir frontend test",
    "test:frontend:coverage": "pnpm --dir frontend test -- --coverage --run",
    "lint:frontend": "pnpm --dir frontend lint",
    "format:frontend": "pnpm --dir frontend format",
    "deps:frontend": "pnpm --dir frontend install --frozen-lockfile",
    "e2e:frontend:install": "pnpm --dir frontend exec playwright install --with-deps chromium",
    "e2e:frontend": "pnpm --dir frontend e2e",
    "ci:frontend": "pnpm run test:frontend:coverage && pnpm run build:frontend",
    "codegen:openapi": "cd backend && ./gradlew generateOpenApiDocs",
    "codegen:frontend": "pnpm --dir frontend api:gen",
    "deps:ml": "cd ml && uv sync",
    "lint:ml": "cd ml && uv run ruff check .",
    "format:ml": "cd ml && uv run ruff format .",
    "format:ml:check": "cd ml && uv run ruff format --check .",
    "typecheck:ml": "cd ml && uv run mypy .",
    "test:ml": "cd ml && uv run pytest",
    "test:ml:coverage": "cd ml && uv run pytest --cov=src --cov-report=term-missing --cov-report=xml:coverage.xml",
    "ci:ml": "pnpm run deps:ml && pnpm run test:ml:coverage",
    "quality:ml": "pnpm run lint:ml && pnpm run format:ml:check && pnpm run typecheck:ml && pnpm run test:ml",
    "check:backend": "pnpm run ci:backend",
    "check:frontend": "pnpm run deps:frontend && pnpm run ci:frontend && pnpm run e2e:frontend",
    "check:ml": "pnpm run ci:ml",
    "ci": "pnpm run ci:backend && pnpm run deps:frontend && pnpm run ci:frontend && pnpm run ci:ml"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.3",
    "@commitlint/config-conventional": "^20.5.3",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "husky": "^9.1.7",
    "jsdom": "^29.0.1",
    "lint-staged": "^16.4.0",
    "prettier": "^3.5.3",
    "vitest": "^4.1.2"
  }
}
